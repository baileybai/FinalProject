<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.recycylerview2020">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DoingTask"
            android:label="Focus on your task"
            android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".FinishedTaskList"
            android:label="Finished Tasks"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ViewFinishedTasks"
            android:label="Finished Task" />
        <activity android:name=".StartNewActivity" />
        <activity
            android:name=".MainActivity"
            android:label="View Your Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>